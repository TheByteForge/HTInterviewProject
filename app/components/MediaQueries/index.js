/**
 *
 * MediaQueries
 * --------------------------------------------
 * Im rather sad I didn't get to incorporate this for media queries
 * in a situation with more time I certainly would have. This media query
 * component allows for you to import it to a page and then extrapolate
 * away media queries to make things more clean and modifiable.
 *
 */

import React from 'react';
import Media from './MediaQueryStyles';

function MediaQueries() {
  return <Media />;
}

MediaQueries.propTypes = {};

export default MediaQueries;
